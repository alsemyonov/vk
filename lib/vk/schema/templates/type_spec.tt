# frozen_string_literal: true
require 'spec_helper'
require '<%= definition.require %>'

RSpec.describe Vk::API::<%= definition.namespace %>::<%= definition.type_name %> do
  subject { described_class }
  it { is_expected.to respond_to :[] }
end
